# dependencies
node_modules/
.pnp
.pnp.*

# next
.next/
out/
next-env.d.ts

# testing
coverage/
test-results/
playwright-report/
blob-report/

# generated staking data
# Chunks live on the `data` branch and are fetched into public/data at build
# time; they must never be committed to a source branch.
/public/data/
/data/

# ingestion checkpoints
.ingest-cache/

# misc
.DS_Store
*.pem
.vercel
.env*
!.env.example
tsconfig.tsbuildinfo
.eslintcache

# Yarn (the project uses npm; leftover from the previous app)
.yarn/
.pnp.*
