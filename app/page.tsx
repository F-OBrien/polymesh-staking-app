/**
 * Placeholder home page. Phase 4 replaces this with the real network pulse
 * (stat tiles, staking-ratio gauge, entry cards) once the chart kit exists.
 */
export default function HomePage() {
  return (
    <main id="main" className="mx-auto max-w-3xl px-4 py-24">
      <h1 className="text-3xl font-semibold tracking-tight">Polymesh Staking Analytics</h1>
      <p className="mt-4 max-w-prose text-secondary">
        Rebuild in progress. The design system and data layer are landing first; charts and pages
        follow.
      </p>
    </main>
  );
}
